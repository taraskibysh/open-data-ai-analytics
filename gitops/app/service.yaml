apiVersion: v1
kind: Service
metadata:
  name: docflow-web-service
  namespace: docflow-system
spec:
  type: NodePort
  selector:
    app: docflow-web
  ports:
    - port: 80
      targetPort: 8866
      nodePort: 30080